# RoomFlow frontend env. Copy to .env. Vite exposes only VITE_* vars.
# Leave empty to use the dev proxy (/api → localhost:5000). In production set the
# deployed API base, e.g. https://roomflow-api.onrender.com/api/v1
VITE_API_URL=/api/v1
